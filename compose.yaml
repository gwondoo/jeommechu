services:
  jeommechu:
    build:
      context: .
    container_name: jeommechu-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./bot.py:/app/bot.py:ro
      - ./kakao.py:/app/kakao.py:ro
      - ./storage.py:/app/storage.py:ro
      - ./data:/app/data
